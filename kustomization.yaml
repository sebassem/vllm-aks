apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Cluster-wide infrastructure (GPU operators, etc.)
  - infrastructure/base
  # Application resources (base without regional customizations)
  - apps/base